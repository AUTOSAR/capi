#  Disclaimer
#
#  This work (specification and/or software implementation) and the material
#  contained in it, as released by AUTOSAR, is for the purpose of information
#  only. AUTOSAR and the companies that have contributed to it shall not be
#  liable for any use of the work.
#
#  The material contained in this work is protected by copyright and other
#  types of intellectual property rights. The commercial exploitation of the
#  material contained in this work requires a license to such intellectual
#  property rights.
#
#  This work may be utilized or reproduced without any modification, in any
#  form or by any means, for informational purposes only. For any other
#  purpose, no part of the work may be utilized or reproduced, in any form
#  or by any means, without permission in writing from the publisher.
#
#  The work has been developed for automotive applications only. It has
#  neither been developed, nor tested for non-automotive applications.
#
#  The word AUTOSAR and the AUTOSAR logo are registered trademarks.
#  --------------------------------------------------------------------------

# ================================================================
#
# File description:
# ----------------
# @file       CMakeLists.txt
# @brief
# @details
# @date       2021-07-19
# @author     jiawei
# @version    1.2.0
#
# ================================================================
#
# This file is generated by ara-gen(1.0.0)
#
# ================================================================

cmake_minimum_required(VERSION 3.4)
project(None) # placeholder only, will be updated to ARA_GEN_EXECUTABLE and its version by aragen-helper.cmake below
# include aragen-helper.cmake -- performs the following:
#   a> Set Executable info defined by arxml to cmake variables prefixed with ARA_GEN_XXX
#   b> Automatically introduce dependent libraries based on ara usage defined by arxml (e.g., ara::per ara::com ara::phm ara::diag...)
#   c> Merge multiple dependent libraries from step b into a single library target named ARA_GEN_BUILD_HELPER_LIBRARY for direct use by the application (see src/CMakeLists.txt)
#   d> Library target ARA_GEN_BUILD_HELPER_LIBRARY will automatically generate ara API code compliant with AP specification based on arxml definitions. The application can directly use it in the current project (include & library path are automatically added)
include(gen/aragen-helper.cmake)

# Set the list of arxml files used by the current project: ARA_GEN_ARXMLS
# Can be passed via cmake parameter (e.g: cmake -DARA_GEN_ARXMLS=/var/xx1/;/var/xx2/)
# Can also be added, removed, or modified here
set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS}
	${ARA_ARXMLS_DIR}/common
	${ARA_ARXMLS_DIR}/development_machines/system
	${ARA_ARXMLS_DIR}/development_machines/machine1/common
	${ARA_ARXMLS_DIR}/development_machines/machine2/common

	${ARA_ARXMLS_DIR}/platform_components/ucm/service_interface
	${ARA_ARXMLS_DIR}/platform_components/ucm/service_interface_someip
	
	${ARA_ARXMLS_DIR}/platform_components/ucmm
	${ARA_ARXMLS_DIR}/development_machines/machine1/processes/ucmmd
	${ARA_ARXMLS_DIR}/development_machines/machine2/processes/ucmmd
	${ARA_ARXMLS_DIR}/platform_components/ucm/service_interface/common.arxml
	${ARA_ARXMLS_DIR}/platform_components/ucmm/service_interface
	

	#only for depends process staus and debug(to gen manifest)
	${ARA_ARXMLS_DIR}/platform_components/com/daemon
	${ARA_ARXMLS_DIR}/development_machines/machine1/processes/nsomeipd/integration/process.arxml   
	${ARA_ARXMLS_DIR}/development_machines/machine2/processes/nsomeipd/integration/process.arxml   
) # arxmls

# Add ARA_GEN_BUILD_HELPER_LIBRARY target
aragen_helper_add_build_helper_library() 

# Add user target(exe) and tests
add_subdirectory(src)

if(ARA_ENABLE_TESTS)
	enable_testing()
	find_package(GTest REQUIRED CONFIG)
	add_subdirectory(test)
endif(ARA_ENABLE_TESTS)