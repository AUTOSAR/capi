#  Disclaimer
#
#  This work (specification and/or software implementation) and the material
#  contained in it, as released by AUTOSAR, is for the purpose of information
#  only. AUTOSAR and the companies that have contributed to it shall not be
#  liable for any use of the work.
#
#  The material contained in this work is protected by copyright and other
#  types of intellectual property rights. The commercial exploitation of the
#  material contained in this work requires a license to such intellectual
#  property rights.
#
#  This work may be utilized or reproduced without any modification, in any
#  form or by any means, for informational purposes only. For any other
#  purpose, no part of the work may be utilized or reproduced, in any form
#  or by any means, without permission in writing from the publisher.
#
#  The work has been developed for automotive applications only. It has
#  neither been developed, nor tested for non-automotive applications.
#
#  The word AUTOSAR and the AUTOSAR logo are registered trademarks.
#  --------------------------------------------------------------------------

# ================================================================
#
# File description:
# ----------------
# @file       CMakeLists.txt
# @brief
# @details
# @date       2021-05-20
# @author     git
# @version    1.2.0
#
# ================================================================
#
# This file is generated by ara-gen(1.0.0)
#
# ================================================================

cmake_minimum_required(VERSION 3.4)

project(None) # Placeholder only, will be updated to ARA_GEN_EXECUTABLE and its version in the following aragen-helper.cmake
set(CMAKE_EXPORT_COMPILE_COMMANDS True)

find_package(ara-sm-internal-utils)
get_target_property(UTILS_INCLUDE_DIRS ara::sm::internal_utils INTERFACE_INCLUDE_DIRECTORIES)

if(NOT DEFINED ARA_SM_ROOT_DIR)
    get_filename_component(ARA_SM_ROOT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/.." ABSOLUTE)
    message(STATUS "ARA_SM_ROOT_DIR not defined, setting to: ${ARA_SM_ROOT_DIR}")
endif()

include_directories(
    ${ARA_SM_ROOT_DIR}/smd/src
    ${UTILS_INCLUDE_DIRS}
)

option(ARA_WITH_V3 "build with v3 content" ON)
option(ARA_WITH_DIAG "build with diag" ON)
option(ARA_WITH_UCM "build with ucm" ON)
option(ARA_WITH_PHM "build with phm" ON)
option(ARA_WITH_SM_COMPILE_PHM "build with sm compile phm" OFF)
option(ARA_WITH_SM_USE_INTERNAL_PHM_MODEL "build with sm compile phm and use phm models in sm misc" OFF) # Only turn on when compiling smd with phm_comm module separately
option(ARA_WITH_NM "build with nm" ON)
option(ARA_ENABLE_NM_USING_COM "interact with nm based ara::com" ON)
option(ARA_WITH_PER "build with per" ON)
option(ARA_ENABLE_TESTS "build with gtest" ON)
option(ARA_TEST_SHELL "build with shell test" OFF)
option(ARA_ENABLE_SM_2311 "enable sm 23-11 features, otherwise 20-11" OFF)
option(ARA_ENABLE_SM_ECU_STATE "enable sm ecu state feature" ON)

IF (ARA_WITH_V3) 
    set(ARA_ENABLE_NM_USING_COM OFF)
endif()

if (NOT ARA_WITH_PHM)
    set(ARA_WITH_SM_COMPILE_PHM OFF)
    set(ARA_WITH_SM_USE_INTERNAL_PHM_MODEL OFF)
endif()

if (NOT ARA_WITH_SM_COMPILE_PHM)
    set(ARA_WITH_SM_USE_INTERNAL_PHM_MODEL OFF)
endif()

if(ARA_WITH_DIAG)
    add_definitions(-DARA_WITH_DIAG)
endif()

if(ARA_WITH_NM)
    add_definitions(-DARA_WITH_NM)
endif()

if(ARA_ENABLE_NM_USING_COM AND ARA_WITH_NM)
    add_definitions(-DARA_ENABLE_NM_USING_COM)
endif()

if(ARA_WITH_UCM)
    add_definitions(-DARA_WITH_UCM)
endif()

if(ARA_WITH_SM_COMPILE_PHM)
    add_definitions(-DARA_WITH_PHM)
endif()

if(ARA_WITH_PER)
    add_definitions(-DARA_WITH_PER)
endif()

if(ARA_TEST_SHELL)
    add_definitions(-DARA_TEST_SHELL)
endif()

if(ARA_ENABLE_SM_2311)
    add_definitions(-DARA_ENABLE_SM_2311)
endif()

if(ARA_ENABLE_SM_ECU_STATE)
    add_definitions(-DARA_ENABLE_SM_ECU_STATE)
endif()

set(STORAGE storage)
set(CONFIG config)
set(DM_COMM dm_comm)
set(ECU_STATE_COMM ecu_state_comm)
set(FG_STATE_COMM fg_state_comm)
set(SM_STATE_COMM sm_state_comm)
set(UCM_COMM ucm_comm)
set(SHELL_COMM shell_comm)
set(EM_COMM em_comm)
set(EVENT_MANAGEMENT event_management)
set(FG_STATE_NOTIFY_COMM fg_state_notify_comm)
set(FG_STATE_NOTIFY_CLIENT fg_state_notify_client)
set(NM_COMM nm_comm)
set(PHM_COMM phm_comm)
set(STATE_MACHINE_MANAGEMENT state_machine_management)

# include aragen-helper.cmake -- perform the following:
#   a> Set the Executable information defined in arxml to cmake variables prefixed with ARA_GEN_XXX
#   b> Automatically introduce dependent libraries (such as ara::per ara::com ara::phm ara::diag....) according to the use of ara by Executable defined in arxml
#   c> Combine the multiple dependent libraries from step b into a single library target named ARA_GEN_BUILD_HELPER_LIBRARY for direct reference by the application (see src/CMakeLists.txt)
#   d> The library target ARA_GEN_BUILD_HELPER_LIBRARY will automatically generate ara API code that conforms to AP specification based on the information defined in arxml, and the application can directly use it in the current project (include & library path are automatically added)

set(ARA_GEN_HELPER_PATH "${CMAKE_CURRENT_SOURCE_DIR}/files/aragen-helper.cmake")
if(EXISTS "${ARA_GEN_HELPER_PATH}" AND NOT IS_DIRECTORY "${ARA_GEN_HELPER_PATH}")
    include(${ARA_GEN_HELPER_PATH})
else()
    include(gen/aragen-helper.cmake)
endif()


set(MAIN_ARXMLS_DIR_PATH ${ARA_ARXMLS_DIR})

if(ARA_ENABLE_TESTS)
    set(MAIN_ARXMLS_DIR_NAME ara-arxmls)
    set(MAIN_ARXMLS_DIR_PATH ${CMAKE_SOURCE_DIR}/${MAIN_ARXMLS_DIR_NAME})

    execute_process(
        COMMAND rm -r ${MAIN_ARXMLS_DIR_PATH}
        WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
        RESULT_VARIABLE result
        OUTPUT_VARIABLE output
        ERROR_VARIABLE error)

    execute_process(
        COMMAND cp -r ${ARA_SYSROOT}/ara/framework/1.0.0/share/${MAIN_ARXMLS_DIR_NAME} ${CMAKE_SOURCE_DIR}/
        WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
        RESULT_VARIABLE result
        OUTPUT_VARIABLE output
        ERROR_VARIABLE error)

    set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS}
            ${CMAKE_CURRENT_SOURCE_DIR}/test
    )

endif(ARA_ENABLE_TESTS)

# Set the list of arxml used by the current project: ARA_GEN_ARXMLS
# Can be passed in by cmake parameters (e.g: cmake -DARA_GEN_ARXMLS=/var/xx1/;/var/xx2/)
# Can also be added, deleted, or modified here
set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS} 
        ${MAIN_ARXMLS_DIR_PATH}/common
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/system
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/common
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/common

        # sm depends on nsomeip_routing
        #only for depends process staus and debug(to gen manifest)
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/com/daemon
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/nsomeipd/integration/process.arxml   
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/nsomeipd/integration/process.arxml   
        
        # sm
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/daemon
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/persistency
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/service_interface_common
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/shell_request
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/state_machine
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/trigger_function_group
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/trigger_state
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/ucm_update_request
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/extension.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/deployment/shell_request
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/deployment/template
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/deployment/service_instance_timing.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/integration/app_deployment.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/integration/development_machine1_mapping.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/integration/kvs_deployment.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/deployment/ucm_update_request
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/deployment/shell_request
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/deployment/template
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/deployment/service_instance_timing.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/integration/app_deployment2.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/integration/development_machine2_mapping.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/integration/kvs_deployment.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/deployment/ucm_update_request
) # arxmls

if(ARA_WITH_DIAG)
    add_definitions(-DARA_WITH_DIAG)
    set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS} 
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/diagnostic_extract
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/com/cg
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/com/extension.arxml
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/diag/daemon
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/diag_ecu_reset
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/power_mode_cg
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/dmd
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/diagnostic_design.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/diagnostic_design.arxml
    )
endif()

if(ARA_WITH_NM)
    add_definitions(-DARA_WITH_NM)
    set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS} 
        # sm depends on nm
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/nm/service_interface
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/nm/service_interface_someip
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/nm_network_state
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/integration/nm_service_mapping.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/integration/nm_service_mapping.arxml
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/deployment/nm
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/deployment/nm
    )
endif()

if(ARA_WITH_UCM)
    add_definitions(-DARA_WITH_UCM)
    set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS} 
        ${MAIN_ARXMLS_DIR_PATH}/platform_components/sm/ucm_update_request
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine1/processes/smd/sm/deployment/ucm_update_request
        ${MAIN_ARXMLS_DIR_PATH}/development_machines/machine2/processes/smd/sm/deployment/ucm_update_request
    )
endif()

if(ARA_WITH_SM_COMPILE_PHM)
    add_definitions(-DARA_WITH_PHM)
    if (ARA_WITH_SM_USE_INTERNAL_PHM_MODEL)
        set(ARA_GEN_ARXMLS ${ARA_GEN_ARXMLS} 
            ${CMAKE_CURRENT_SOURCE_DIR}/misc/phm_model/phm_recovery_action
        )
    endif()
endif()

if(ARA_TEST_SHELL)
    add_definitions(-DARA_TEST_SHELL)
endif()

# Add ARA_GEN_BUILD_HELPER_LIBRARY target
aragen_helper_add_build_helper_library() 

add_subdirectory(src)


if(ARA_ENABLE_TESTS)
	enable_testing()
	find_package(GTest REQUIRED CONFIG)
	add_subdirectory(test)
endif(ARA_ENABLE_TESTS)

# Only install source code when building ap-all
if(ARA_ENABLE_AP_ENV)
    #install ara-sm sources 
    install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/
        DESTINATION ${CMAKE_INSTALL_DATAROOTDIR}/ara-sm/source
        COMPONENT dev
        PATTERN ".git*" EXCLUDE
        PATTERN ".vscode*" EXCLUDE
        PATTERN "build*" EXCLUDE
        PATTERN "qac*" EXCLUDE
        PATTERN "doc*" EXCLUDE
        PATTERN "test*" EXCLUDE
    )
endif()
