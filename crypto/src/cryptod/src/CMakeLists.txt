#  Disclaimer
#
#  This work (specification and/or software implementation) and the material
#  contained in it, as released by AUTOSAR, is for the purpose of information
#  only. AUTOSAR and the companies that have contributed to it shall not be
#  liable for any use of the work.
#
#  The material contained in this work is protected by copyright and other
#  types of intellectual property rights. The commercial exploitation of the
#  material contained in this work requires a license to such intellectual
#  property rights.
#
#  This work may be utilized or reproduced without any modification, in any
#  form or by any means, for informational purposes only. For any other
#  purpose, no part of the work may be utilized or reproduced, in any form
#  or by any means, without permission in writing from the publisher.
#
#  The work has been developed for automotive applications only. It has
#  neither been developed, nor tested for non-automotive applications.
#
#  The word AUTOSAR and the AUTOSAR logo are registered trademarks.
#  --------------------------------------------------------------------------

# ================================================================
#
# File description:
# ----------------
# @file       CMakeLists.txt
# @brief
# @details
# @date       2022-09-27
# @author     hanjingjing
# @version    1.2.0
#
# ================================================================
#
# This file is generated by ara-gen(1.0.0) based on arxml
# Set the root directory of crypto module source code
#
# ================================================================

get_filename_component(CRYPTO_ROOT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../../../" ABSOLUTE)
# Add common dependencies
include(${CRYPTO_ROOT_DIR}/src/misc/cmake/dependencies.cmake)
setup_find_packages(IN_DEAMON WITH_IAM)

set(DAEMON_NAME ${ARA_GEN_EXECUTABLE})

message(STATUS "======== Begin : ${PROJECT_NAME}.${DAEMON_NAME} ========")
message(STATUS " ***  CRYPTO_ROOT_DIR           = ${CRYPTO_ROOT_DIR}")
message(STATUS "  **  CMAKE_CURRENT_SOURCE_DIR  = ${CMAKE_CURRENT_SOURCE_DIR}")

file(GLOB_RECURSE SRC_PH_LIST CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
print_source_list("${SRC_PH_LIST}" "srcFile")

add_executable(${DAEMON_NAME} ${SRC_PH_LIST})
# Check if ARA_CRYPTO_DEBUG macro is defined and pass it to C++
add_precompiled_macros(${DAEMON_NAME})

set_target_properties( ${DAEMON_NAME}
PROPERTIES
    OUTPUT_NAME ${ARA_GEN_EXECUTABLE}
)

target_include_directories(${DAEMON_NAME} 
PRIVATE
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    $<BUILD_INTERFACE:${CRYPTO_ROOT_DIR}/src/ara_crypto/include>
	$<BUILD_INTERFACE:${CRYPTO_ROOT_DIR}/src/lib_isoft/include>
)

# 5. Add precompile macros to be passed to C++
if(ARA_WITH_IAM)
	target_compile_definitions(${DAEMON_NAME}
		PRIVATE
			ARA_WITH_IAM=1
	)
endif()

# System paths need to be written separately: only this way can suppress warnings
target_include_directories(${DAEMON_NAME}
SYSTEM PRIVATE
	${OPENSSL_INCLUDE_DIR}  # Use OpenSSL header files
	$<BUILD_INTERFACE:${CMAKE_INSTALL_PREFIX}/include>
)

# Linking
set_link_libraries( ${DAEMON_NAME} IN_DEAMON WITH_IAM EXTRA_LIBS ${ARA_GEN_BUILD_HELPER_LIBRARY})

add_dependencies( ${DAEMON_NAME} ${ARA_GEN_BUILD_HELPER_LIBRARY} ara::ara_crypto)
# add_dependencies(${DAEMON_NAME} install_crypto)

# isoft-ap unified handling of targets
aragen_helper_register_target(TARGET ${DAEMON_NAME} TYPE EXE)

message(STATUS "======== End   : ${PROJECT_NAME}.${DAEMON_NAME} ========")